CREATE TABLE paises (
                        id INT NOT NULL AUTO_INCREMENT,
                        nome VARCHAR(255) NOT NULL,
                        bandeira VARCHAR(255),
                        PRIMARY KEY (id)
);
