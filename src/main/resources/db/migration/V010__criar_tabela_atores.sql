CREATE TABLE atores (
                        id INT NOT NULL AUTO_INCREMENT,
                        nome VARCHAR(255) NOT NULL,
                        data_nascimento DATE,
                        PRIMARY KEY (id)
);
